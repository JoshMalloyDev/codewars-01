function basicOp(operation, val1, val2)
{
  // just create a bunch of if statments 
 if (operation == '+') return val1 + val2
  if (operation == '-') return val1 - val2
  if (operation == '*') return val1 * val2
  if (operation == '/') return val1 / val2
  // Code
}