function abbrevName(name){
    let nam = name.split(" ")
   return nam[0].charAt(0).toUpperCase()+'.' + nam[1].charAt(0).toUpperCase();
      // code away
  
  }

  function abbrevName(name){
    let nam = name.split(' ')
    return nam[0].charAt(0).toUpperCase()+'.' + nam[1].charAt(0).toUpperCase();
    
    }

    // worked with mark on this one