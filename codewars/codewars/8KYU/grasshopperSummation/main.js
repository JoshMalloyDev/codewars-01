let summation = function (num) {
    // we set a variable of answers to zero
    let answer = 0
    // used a forloop to loop through all varibales up to 
    // the paramator of num 
  for (let i = 1; i <= num; i++) {
    // incrimented the answer
    answer += i;
  }
    return answer
    }